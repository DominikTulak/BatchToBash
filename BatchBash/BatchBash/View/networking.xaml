<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BatchBash.View.networking">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
            <Label Text="{Binding Nadpis}" FontSize="Large" HorizontalOptions="Center"/>
            <Button Text="(ping) Test odezvy" Command="{Binding ChangeViewping}" />
            <Button Text="(ifconfig) Nastavení síťových rozhraní" Command="{Binding ChangeViewifconfig}" />
            <Button Text="(iwconfig) Nastavení bezdrátových rozhraní" Command="{Binding ChangeViewiwconfig}" />
            <Button Text="(rfkill) Nastavování zámku bezdrátových adaptérů" Command="{Binding ChangeViewrfkill}" />
            <Button Text="(dhclient) Nastavení DHCP klienta" Command="{Binding ChangeViewdhclient}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>