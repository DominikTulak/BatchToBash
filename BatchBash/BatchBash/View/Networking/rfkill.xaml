<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BatchBash.View.Networking.rfkill">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout VerticalOptions="StartAndExpand" HorizontalOptions="Center">
                <Label Text="(rfkill) Nastavení zámku bezdrátových zařízení" FontSize="Large"/>
                <Entry Placeholder="Název zařízení" Text="{Binding inputText}"/>
                <Label Text="Stav (vypnuto/zapnuto)" FontSize="Medium" />
                <Switch IsToggled="{Binding state}"/>
                <Label Text="Spustit jako root" FontSize="Medium" />
                <Switch IsToggled="{Binding sudo}"/>
            </StackLayout>
            <StackLayout VerticalOptions="End" HorizontalOptions="Center">
                <Label Text="{Binding output}" FontSize="Large"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>