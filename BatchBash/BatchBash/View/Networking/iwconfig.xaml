<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BatchBash.View.Networking.iwconfig">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout VerticalOptions="StartAndExpand" HorizontalOptions="Center">
                <Label Text="(iwconfig) Nastavení bezdrátových zařízení" FontSize="Large"/>
                <Label Text="Zobrazit i vypnuté zařízení" FontSize="Medium" />
                <Switch IsToggled="{Binding all}"/>
                <Label Text="Spustit jako root" FontSize="Medium" />
                <Switch IsToggled="{Binding sudo}"/>
            </StackLayout>
            <StackLayout VerticalOptions="End" HorizontalOptions="Center">
                <Label Text="{Binding output}" FontSize="Large"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>